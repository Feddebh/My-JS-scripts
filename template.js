// template string

const numero = (num1, num2) =>  (
    `el numero es: ${num1 + num2}` // RETORNA la suma de los numeros que le pasamos a la constante
    ) 
const resultado = numero(15, 20);

console.log(resultado)
